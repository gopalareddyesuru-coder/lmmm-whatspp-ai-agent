services:
  - type: web
    name: lmmm-whatsapp-ai-agent
    runtime: node
    buildCommand: npm install && npm run verify:runtime
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
