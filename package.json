{
  "name": "lmmm-ai-maintenance",
  "version": "8.15.4",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "verify:runtime": "node --check app.js && node -e \"import('mdb-reader').then(()=>console.log('mdb-reader runtime OK')).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "engines": {"node": ">=20"},
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "mdb-reader": "3.2.0",
    "pg": "^8.16.3"
  }
}
